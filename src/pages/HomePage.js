import React from 'react';
import FormList from '../components/FormList';

const HomePage = () => {
    return (
        <div>
            <FormList />
        </div>
    );
};

export default HomePage;
